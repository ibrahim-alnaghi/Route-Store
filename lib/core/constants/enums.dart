
enum TextSizes { small, medium, large }
enum SnackBarType {
  error,
  warning,
  success,
  info,
  networkFailure,
  networkSuccess,
}
enum RequestStates {
  initial,
  loading,
  success,
  failure,
}

